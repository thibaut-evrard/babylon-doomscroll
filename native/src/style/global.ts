import { StyleSheet } from "react-native";

export const globalStyle = StyleSheet.create({
  container: {
    height: "100%",
    width: "100%",
  },
  text: {
    pointerEvents: "none",
    userSelect: "none",
  },
});
